func (int) main()
{
    int i=0,a;
    // Declaration of iterator outside the loop
    for(i=0;i<10;i++)
    {
        a=i;
    }
    //Declaration of iterator inside the loop
    for(int i=0;i<10;i++)
    {
        a=i;
    }
}